plugins { id 'com.android.application' }

android {
    namespace 'dev.yuki.glyphtoy'
    compileSdk 36

    defaultConfig {
        applicationId 'dev.yuki.glyphtoy'
        minSdk 34
        targetSdk 36
        versionCode 1
        versionName '0.1.0'
    }
}

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar', '*.aar'])
}
